// project import
import pages from './pages';
import dashboard from './dashboard';
import stage from './stage';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, pages, stage]
};

export default menuItems;
